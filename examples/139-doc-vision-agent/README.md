---
teaching_ready: true
---
# 139 — Document Vision Agent

Renders each page of a PDF to PNG, sends it to a vision LLM, and extracts structured JSON data. Shows how to combine `pypdfium2` page rendering with vision models for document intelligence.

**Run:** `python main.py` — requires `OPENAI_API_KEY` in `.env`
