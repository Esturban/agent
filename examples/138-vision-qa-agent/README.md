---
teaching_ready: true
---
# 138 — Vision Q&A Agent

Sends an image (URL or local path) plus a text question to a vision LLM and returns the answer. Demonstrates multimodal input via base64 encoding and the OpenAI vision API.

**Run:** `python main.py` — requires `OPENAI_API_KEY` in `.env`
